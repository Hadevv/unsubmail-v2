# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

UnsubMail v2 - A Rust project that is currently in early initialization phase. The repository structure and architecture will be defined as development progresses.

## Development Setup

This project uses Rust and Cargo. Standard Cargo commands will be used once the project structure is established:

- `cargo build` - Build the project
- `cargo test` - Run tests
- `cargo run` - Run the application
- `cargo check` - Check code without building
- `cargo fmt` - Format code
- `cargo clippy` - Run linter

## Project Structure

The project structure has not yet been established. This section will be updated as the architecture is defined.
